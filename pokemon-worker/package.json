{
  "name": "pokemon-script-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute POKEMON_DB --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute POKEMON_DB --remote --file=schema.sql"
  }
}
